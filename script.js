const taks = {
    lista: [
        {
            descricao: "teste",
            check: false
        }
    ]

}

let 


function crairTaks(descricao) {
    taks.lista.push({descricao: descricao, check: false})
    let Taks = document.createAttribute('li')
    Taks.style.height = "10vh"
    Taks.style.background = "red"
}

function atualizarTaks() {
    
}

function deltarTaks() {
    
}